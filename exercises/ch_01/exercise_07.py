# Translate the following jpseudocode to code, using static or nonstatic 
# function where appropriate:
#
# - The vector `v` equal (1, 5).
# - The vector `u` equals `v` multiplied by 2.
# - The vector `w` equals `v` minus `u`.
# - Divide the vector `w` by 3

from p5 import *

v = Vector(1, 5)
u = v * 2
w = v - u
v / 3
